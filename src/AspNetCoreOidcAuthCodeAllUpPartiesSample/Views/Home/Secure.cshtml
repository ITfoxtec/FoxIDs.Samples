@using Microsoft.AspNetCore.Authentication;
@using Microsoft.IdentityModel.Protocols.OpenIdConnect;
@{
    ViewData["Title"] = "Secure";
}
<h2>@ViewData["Title"]</h2>
<strong>The page require an authenticated user</strong>

<p>
    <h4>ID token claims</h4>
    <dl>
        @foreach (var claim in User.Claims)
        {
            <dt>@claim.Type</dt>
            <dd style="padding-left: 10px">@claim.Value</dd>
        }
    </dl>

    <h4>User tokens</h4>
    <dl>
        <dt>id token</dt>
        <dd>@await ViewContext.HttpContext.GetTokenAsync(OpenIdConnectParameterNames.IdToken)</dd>

        <dt>access token</dt>
        <dd>@await ViewContext.HttpContext.GetTokenAsync(OpenIdConnectParameterNames.AccessToken)</dd>

        <dt>refresh token</dt>
        <dd>@await ViewContext.HttpContext.GetTokenAsync(OpenIdConnectParameterNames.RefreshToken)</dd>
    </dl>
</p>
